#include <bits/stdc++.h>

using namespace std;

int main() {
/*
    int n;
    int ar[100010];
    int queries;
    int q;
    int l=0, r=0, f=0;

    cin>>n;

    for(int i=0;i<n;i++)
    {
        cin>>ar[i];
    }

    cin>>queries;

    for(int i=0;i<queries;i++)
    {
        cin>>q;
        for(int x=0;x<n;x++)
        {
            if(ar[x]==q)
            {
                l+=(x+1);
                f++;
            }

            if(ar[n-1-x]==q)
            {
                r+=(x+1);
                f++;
            }

            if(f==2)
            {
                f=0;
                break;
            }
        }
    }

    cout<<l<<" "<<r<<endl;
*/

    

    return 0;
}